package serialization;

import java.io.FileInputStream;
import java.io.ObjectInputStream;

public class PersonDeserializationDemo {

	public static void main(String[] args) {
		
		try {
			FileInputStream fis = new FileInputStream("D://Users//Rice//Documents//CogentWorkspace//TextFiles//Person.ser");
			ObjectInputStream ois = new ObjectInputStream(fis);
			Object obj = ois.readObject();
			while (obj != null)
			{
				Person person = (Person)obj;
				System.out.println("Employee ID: " + person.getRollNum());
				System.out.println("Employee Name: " + person.getName());
				System.out.println("Employee Salary: " + person.getAge());
				System.out.println("Employee SSN: " + emp.ssn);
			}
			fis.close();
			ois.close();
		} catch (Exception e) {
			System.out.println(e);
		}
		
	}
	
}
